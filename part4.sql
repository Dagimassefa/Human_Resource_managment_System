use HumanResource
create table salary(
Emp_ID varchar(30),
salary float,
Effective_date date
)
select * from salary
create table position(
Emp_ID varchar(30),
position varchar(100),
Effective_date date
)
select * from position
